<launch>
  
  <arg name="launch_prefix" default="" /> <!-- set to value="gdbserver localhost:10000" for remote debugging -->
  <arg name="node_namespace" default="apriltag_ros_continuous_node" />
  <arg name="camera_name" default="/camera/color" />
  <arg name="camera_frame" default="camera_aligned_depth_to_color_frame" />
  <arg name="image_topic" default="image_raw" />

  <arg name="launch_rviz" default="True" doc="Option to launch RViz"/>

  <!-- Set parameters -->
  <rosparam command="load" file="$(find vision)/config/settings.yaml" ns="$(arg node_namespace)"/>
  <rosparam command="load" file="$(find vision)/config/tags.yaml" ns="$(arg node_namespace)"/>

  <include file="$(find realsense2_camera)/launch/rs_camera.launch">
      <arg name="filters" value="pointcloud"/>
      <arg name="align_depth" value="true"/>
  </include>

  <node name="rviz" type="rviz" pkg="rviz" args="-d $(find vision)/config/face_detection.rviz" if="$(arg launch_rviz)"/>

  <node name="image_view" type="image_view" pkg="image_view">
    <param name="image" value="/tag_detections_image"/>
  </node>
  
  <node pkg="apriltag_ros" type="apriltag_ros_continuous_node" name="$(arg node_namespace)" clear_params="true" output="screen" launch-prefix="$(arg launch_prefix)" >
    <!-- Remap topics from those used in code to those on the ROS network -->
    <remap from="image_rect" to="$(arg camera_name)/$(arg image_topic)" />
    <remap from="camera_info" to="$(arg camera_name)/camera_info" />

    <param name="camera_frame" type="str" value="$(arg camera_frame)" />
    <param name="publish_tag_detections_image" type="bool" value="true" />      <!-- default: false -->
  </node>

</launch>
